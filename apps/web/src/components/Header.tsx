import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="mb-12 flex justify-between items-center">
            <h1 className="text-xl font-medium tracking-tight text-stone-800">auri</h1>
        </header>
    );
};

export default Header;
