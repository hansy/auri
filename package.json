{
  "name": "dictation-app-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter \"*\" dev",
    "build": "bun run --filter \"*\" build"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "typescript": "~5.8.2"
  }
}